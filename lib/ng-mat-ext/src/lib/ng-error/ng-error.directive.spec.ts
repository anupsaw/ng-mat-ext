import { NgErrorDirective } from './ng-error.directive';

describe('NgErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgErrorDirective();
    expect(directive).toBeTruthy();
  });
});
